import { ScrollView, Text} from "react-native";

const CureHistory = () => {
    return(
        <ScrollView>
            <Text>Lịch sử khám bệnh</Text>
        </ScrollView>
    )
}

export default CureHistory;